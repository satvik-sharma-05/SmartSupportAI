# Minimal requirements for free tier deployment (< 512MB RAM)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Lightweight ML (no PyTorch - use sklearn instead)
scikit-learn>=1.3.0
joblib>=1.3.0

# Database (optional - remove if not needed)
# pymongo>=4.6.0

# Utilities
numpy>=1.24.0
